apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: terminus
resources:
  - deployment.yaml
  - service.yaml
  - httproute.yaml
  - 1password.secret.yaml
  - storage.yaml

images:
  - name: terminus
    newName:  ghcr.io/usetrmnl/terminus
    newTag: "0.56.0"