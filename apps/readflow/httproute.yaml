apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: readflow-httproute
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: traefik-gateway
      namespace: default
  hostnames:
    - readflow.x.pmcd.io
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: readflow
          kind: Service
          namespace: default
          port: 80