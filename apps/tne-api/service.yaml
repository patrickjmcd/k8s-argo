apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/instance: tne-api
    app.kubernetes.io/name: tne-api
  name: tne-api
  namespace: default
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: http
  selector:
    app.kubernetes.io/instance: tne-api
    app.kubernetes.io/name: tne-api
  type: ClusterIP
