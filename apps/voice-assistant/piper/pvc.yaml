apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wyoming-piper-data
  namespace: voice-assistant
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi
