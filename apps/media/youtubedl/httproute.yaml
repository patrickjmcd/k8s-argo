apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: youtubedl-httproute
  namespace: media
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: traefik-gateway
      namespace: default
  hostnames:
    - youtubedl.x.pmcd.io
    - youtube.x.pmcd.io
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: youtubedl
          kind: Service
          namespace: media
          port: 80