apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - service.yaml

configMapGenerator:
  - name: radarr-config
    namespace: media
    files:
      - config.xml

images:
  - name: radarr
    newName: linuxserver/radarr
    newTag: version-4.4.4.7068
