apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: radarr
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`radarr.x.pmcd.io`)
      kind: Rule
      services:
        - name: radarr
          port: 80
  tls:
    certResolver: pmcdio