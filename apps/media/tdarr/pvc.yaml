---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tdarr-config-server-pvc
  namespace: media
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs-config-tdarr-server
  volumeName: nfs-config-tdarr-server
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tdarr-config-config-pvc
  namespace: media
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs-config-tdarr-config
  volumeName: nfs-config-tdarr-config

