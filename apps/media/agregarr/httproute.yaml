apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: agregarr-httproute
  namespace: media
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: traefik-gateway
      namespace: default
  hostnames:
    - agregarr.x.pmcd.io
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: agregarr
          kind: Service
          namespace: media
          port: 80