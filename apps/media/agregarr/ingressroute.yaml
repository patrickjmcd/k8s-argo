apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: agregarr
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`agregar.x.pmcd.io`)
      kind: Rule
      services:
        - name: agregarr
          port: 80
  tls:
    certResolver: pmcdio