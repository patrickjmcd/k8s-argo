apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sab-incomplete-pvc
  namespace: media
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 2Ti
  storageClassName: nfs-csi-hot
