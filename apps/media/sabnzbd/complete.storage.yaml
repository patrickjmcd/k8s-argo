apiVersion: v1
kind: PersistentVolume
metadata:
  name: sab-complete-pv
spec:
  capacity:
    storage: 500Gi           # logical size; NFS itself enforces real size
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""       # IMPORTANT: empty string for static PV
  mountOptions:
    - hard
    - nolock
    - nfsvers=3
  csi:
    driver: nfs.csi.k8s.io
    volumeHandle: sab-complete-pv   # just a unique ID string
    volumeAttributes:
      server: 192.168.1.253               # NFS server IP/hostname
      share: /var/nfs/shared/Media/downloads/complete
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sab-complete-pvc
  namespace: media
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 500Gi
  storageClassName: ""       # IMPORTANT: empty string to bind to static PV
  volumeName: sab-complete-pv  # bind to the specific static PV
