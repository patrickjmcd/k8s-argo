apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
#  - helm-release.yaml
  - deployment.yaml
  - service.yaml

configMapGenerator:
  - name: sonarr-config
    namespace: media
    files:
      - config.xml

images:
  - name: sonarr
    newName: linuxserver/sonarr
    newTag: 4.0.0-develop
