apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: sonarr
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`sonarr.x.pmcd.io`)
      kind: Rule
      services:
        - name: sonarr
          port: 80
  tls:
    certResolver: pmcdio