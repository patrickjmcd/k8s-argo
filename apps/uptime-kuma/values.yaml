image:
  repository: louislam/uptime-kuma
  tag: "2.4.0"

service:
  port: 3001

resources:
  requests:
    cpu: 35m
    memory: 283Mi
  limits:
    cpu: 49m
    memory: 288Mi

probes:
  readiness:
    type: httpGet
    path: /
    initialDelaySeconds: 10
    periodSeconds: 30
    timeoutSeconds: 10
    failureThreshold: 3
  liveness:
    enabled: false
  startup:
    enabled: false

httproute:
  enabled: true
  hostnames:
    - status.x.pmcd.io
    - uptime-kuma.x.pmcd.io

affinity:
  nodeAffinity:
    requiredDuringSchedulingIgnoredDuringExecution:
      nodeSelectorTerms:
        - matchExpressions:
            - key: node-role.kubernetes.io/control-plane
              operator: DoesNotExist

persistence:
  enabled: true
  type: longhorn
  size: 1Gi
  mountPath: /app/data
  labels:
    recurring-job-group.longhorn.io/b2-backup: enabled

serviceMonitor:
  enabled: true
  path: /metrics
  basicAuth:
    enabled: true
    secretName: uptime-kuma-1pw
    usernameKey: username
    passwordKey: password
