apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: longhorn-hivemq-data
  namespace: hivemq
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 2Gi
