apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: nats

resources:
  - namespace.yaml
  - loadbalancer-service.yaml
  - monitor-service.yaml
  - httproute.yaml
  - metrics.yaml
