apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: ia-ignition-httproute
  namespace: default
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: traefik-gateway
      namespace: default
  hostnames:
    - ia-ignition.x.pmcd.io
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: ia-ignition
          kind: Service
          namespace: default
          port: 8088