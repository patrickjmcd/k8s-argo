apiVersion: v1
kind: Namespace
metadata:
  name: adsb
  labels:
    # Prometheus only discovers ServiceMonitors in namespaces with this label
    # (serviceMonitorNamespaceSelector) — needed for the adsb-exporter monitor.
    prometheus: enabled
