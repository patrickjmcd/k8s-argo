apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- deployment.yaml
- service.yaml

images:
- name: tronbyt-server
  newName:  ghcr.io/tronbyt/server
  newTag: latest

configMapGenerator:
  - name: tronbyt-server-config
    files:
      - .env
