apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: tronbyt-httproute
  namespace: default
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: traefik-gateway
      namespace: default
  hostnames:
    - tronbyt.x.pmcd.io
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: tronbyt-server
          kind: Service
          namespace: default
          port: 8888