apiVersion: longhorn.io/v1beta2
kind: Node
metadata:
  name: kube-leader
  namespace: longhorn-system
  finalizers:
    - longhorn.io
  annotations:
    argocd.argoproj.io/sync-options: Replace=true
spec:
  # etcd/control-plane node: keep Longhorn replicas off its disk so replica
  # write bursts don't compete with etcd fsync latency (SATA SSD). Pods still
  # run here and can mount Longhorn volumes hosted elsewhere.
  allowScheduling: false
  disks:
    default-disk-f60ce8e5937d162e:
      allowScheduling: false
      diskDriver: ""
      diskType: filesystem
      evictionRequested: false
      path: /var/lib/longhorn/
      storageReserved: 12012114739
      tags: []
  # Phase 2c: drain replicas onto the NVMe Pis ahead of the pve13 conversion.
  evictionRequested: true
  instanceManagerCPURequest: 0
  name: kube-leader
  tags: []
