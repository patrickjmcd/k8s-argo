apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: kubewall
  namespace: kubewall
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`kubewall.x.pmcd.io`)
      kind: Rule
      services:
        - name: kubewall
          port: 8443
  tls:
    certResolver: pmcdio