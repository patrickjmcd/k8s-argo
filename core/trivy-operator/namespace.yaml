apiVersion: v1
kind: Namespace
metadata:
  name: trivy-system
  labels:
    # Prometheus only discovers ServiceMonitors in namespaces with this label
    # (its serviceMonitorNamespaceSelector). Without it, the chart's
    # ServiceMonitor is ignored.
    prometheus: enabled
