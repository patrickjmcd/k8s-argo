apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: longhorn-prometheus-data
  namespace: prometheus
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 2Gi